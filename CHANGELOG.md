## 1.0.0

* Initial release with support for requesting in-app reviews.
* Added `isAvailable()` to check if the review flow is available.
* Added `openStoreListing()` to open the app's store page as a fallback.
* Android support implemented.
